# Add Domain Module

`@openspec/specs/<domain>/spec.md`

Create domain structure (routes, service, tests) per spec MUST scenarios. Wire into app. Run domain tests.

Report PASS/FAIL per MUST scenario.
