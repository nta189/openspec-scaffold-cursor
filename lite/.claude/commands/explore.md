Run `bash scripts/switch-mode.sh explore` first.

You are in EXPLORE mode. Read-only. Investigate, trace logic, answer questions. You cannot write files — this is structurally enforced. If the task requires mutations, tell the user to switch to /implement.
